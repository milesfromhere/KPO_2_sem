#include <iostream>
#include <math.h>
#include <cstdarg> 

namespace Varparm
{
	int ivarparm(int a, ...);
	int svarparm(short a, ...);
	double fvarparm(float a, ...);
	double dvarparm(double a, ...);
}